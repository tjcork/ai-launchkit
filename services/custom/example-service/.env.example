# Service Configuration
# ---------------------

# Repository URL to clone and build
SERVICE_GIT_REPO='https://github.com/yourusername/your-repo.git'

# Branch to track (default: main)
SERVICE_GIT_BRANCH='main'

# Optional: Private Git Token (for private repos)
SERVICE_GIT_TOKEN=''

# Port the application listens on inside the container
SERVICE_PORT=3000

# Port to expose on the host
SERVICE_HOST_PORT=5050

# Optional: Custom start command (overrides Dockerfile CMD)
SERVICE_COMMAND=''

# Optional: Custom image name
SERVICE_IMAGE_NAME='example-service:latest'

# Application Environment Variables
# ---------------------------------
# Define which variables from this file are passed to the application.
# 
# 1. RUNTIME_ENV_PASSTHROUGH
#    Variables matching these patterns are added to the container's runtime environment
#    (and the .env file on disk).
#    Default: '*' (All variables, including SERVICE_*, be careful!)
#    Example: 'MYAPP_* DATABASE_*'
#
# 2. BUILD_ARGS
#    Variables matching these patterns are passed as --build-arg to 'docker build'.
#    Default: 'VITE_* NEXT_* PUBLIC_* REACT_* NUKS_*'
#    Example: 'MY_BUILD_VAR_*'

# RUNTIME_ENV_PASSTHROUGH='*'
# BUILD_ARGS='VITE_* NEXT_*'

# Example App Config
# SERVICE_ALLOWED_HOSTS='example.com,localhost'
# VITE_API_URL='https://api.example.com'
# DATABASE_URL='postgres://...'
