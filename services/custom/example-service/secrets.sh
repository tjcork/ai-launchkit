#!/bin/bash
source "$PROJECT_ROOT/lib/utils/secrets.sh"

# No specific secrets generation required by default 
# but this file ensures the service follows CoreKit standards.
# Add any custom secret generation here if needed.
