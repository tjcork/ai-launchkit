services:
  landing-page:
    image: nginx:alpine
    container_name: landing-page
    env_file:
      - .env
    profiles:
      - landing-page
    restart: unless-stopped
    volumes:
      - ./website:/usr/share/nginx/html:ro
    networks:
      default:
        aliases:
          - landing-page

