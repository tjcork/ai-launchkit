{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Service Definition",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Unique identifier for the service"
    },
    "display_name": {
      "type": "string",
      "description": "Human-readable name for documentation"
    },
    "rank": {
      "type": "integer",
      "description": "Sort order (lower is higher in list). Default: 100",
      "default": 100
    },
    "category": {
      "type": "string",
      "description": "Display category for the service"
    },
    "description": {
      "type": "string",
      "description": "Short description of the service"
    },
    "interface": {
      "type": "string",
      "description": "Primary URL/Interface pattern (e.g., service.<yourdomain>.com)"
    },
    "source": {
      "type": "string",
      "description": "Upstream source URL (GitHub, etc.)"
    },
    "depends_on": {
      "description": "Service dependencies",
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["name", "category", "description"]
}
