# CoreDNS private resolver
# PRIVATE_BASE_DOMAIN is needed to build defaults; set this to your domain.
PRIVATE_BASE_DOMAIN=yourdomain.com
# Bind IP must exist on the host (e.g., 10.255.0.5/32 on lo)
PRIVATE_DNS_TARGET_IP=10.255.0.5
# Space-separated hostnames to resolve to the bind IP
PRIVATE_DNS_HOSTS="mail.${PRIVATE_BASE_DOMAIN} ssh.${PRIVATE_BASE_DOMAIN} ${PRIVATE_BASE_DOMAIN%%.*}.local"
# Upstream forwarders
PRIVATE_DNS_FORWARD_1=1.1.1.1
PRIVATE_DNS_FORWARD_2=1.0.0.1
