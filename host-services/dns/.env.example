# IP to bind DNS on (must exist on host). Example: 10.255.0.5
PRIVATE_DNS_TARGET_IP=10.255.0.5

# Space-separated hostnames to resolve to PRIVATE_DNS_TARGET_IP
# Will be rendered into Corefile hosts line
PRIVATE_DNS_HOSTS="mail.${BASE_DOMAIN:-example.com} ssh.${BASE_DOMAIN:-example.com}"

# Upstream forwarders
PRIVATE_DNS_FORWARD_1=1.1.1.1
PRIVATE_DNS_FORWARD_2=1.0.0.1
